'use strict'

// const path = require('path')

// const sassPath = [ path.resolve(__dirname, '../src/scss') ]

module.exports = {
  // sass: `css!sass?includePaths=${sassPath}`
  // postcss: [
  //   require('autoprefixer')({
  //     browsers: ['last 3 versions']
  //   })
  // ]
}
